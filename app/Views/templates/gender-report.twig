<div class="wrap">
    <h1>{{ page_title }}</h1>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    {% include 'report-styles.twig' %}

    <div class="postbox">
        <div class="inside">
            <div class="donut-chart" style="max-width: 450px; margin: 0 auto;">
                {% if gender_data %}
                    <canvas id="genderChart"></canvas>
                    <table class="wp-list-table widefat striped">
                        <tbody>
                            {% for item in gender_data %}
                                <tr>
                                    <td>{{ item.gender }}</td>
                                    <td><strong>{{ item.count }}</strong></td>
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                {% else %}
                    <p>No gender data available.</p>
                {% endif %}
            </div>
        </div>
    </div>
</div>

<script>
{% if gender_data %}
new Chart(document.getElementById('genderChart'), {
    type: 'doughnut',
    data: {
        labels: [{% for item in gender_data %}'{{ item.gender|e }}'{% if not loop.last %},{% endif %}{% endfor %}],
        datasets: [{
            data: [{% for item in gender_data %}{{ item.count }}{% if not loop.last %},{% endif %}{% endfor %}],
            backgroundColor: ['#FF6384', '#36A2EB', '#FFCE56', '#4BC0C0', '#9966FF', '#FF9F40', '#C9CBCF'],
            borderWidth: 2,
            borderColor: '#fff'
        }]
    },
    options: { 
        responsive: true, 
        maintainAspectRatio: false,
        plugins: {
            legend: { position: 'bottom' }
        }
    }
});
{% endif %}
</script>
